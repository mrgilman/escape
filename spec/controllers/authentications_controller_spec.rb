require 'spec_helper'

describe AuthenticationsController do
  # describe "#create" do
  #   before :each do
  #     @user = Fabricate(:user)
  #     login_user
  #   end

  #   it "creates a new authentication" do
  #     Authentication.count.should == 0
  #     auth = {:provider => "tripit", :token => "abc123", :secret => "123abc"}
  #     post :create
  #     Authentication.count.should == 1
  #   end
  # end

  # describe "add_authentication" do
  #   before :each do
  #     @user = Fabricate(:user)
  #     login_user
  #   end

  #   it "creates a new authentication" do
  #     Authentication.count.should == 0
  #     auth = {:provider => "tripit", :token => "abc123", :secret => "123abc"}
  #     add_authentication(auth)
  #     Authentication.count.should == 1
  #   end
  # end
end
